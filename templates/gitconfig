[user]
    name = {{USERNAME}}
    email = {{EMAIL}}
[alias]
    ci = commit
    cim = commit -m
    a = add
    c = checkout
    s = status
    b = branch
    d = diff
    p = push -u
    w = whatchanged
    g = "!g() { \
            git grep $1 ':(exclude)*test.js' ':(exclude)*test*.py'; \
        }; g"
[core]
    pager = less -FRSX
    editor = vim
    excludesfile = ~/.gitignore_global
[color]
    ui = auto
    pager = true
[mergetool]
    keepBackup = false
[pull]
    rebase = true
[url "git@github.com:"]
    insteadOf = https://github.com/
[cola]
    spellcheck = false
